# deploy.yml — The control plane for deployments
# Set enabled: true to deploy an app, false to take it offline

domain: ryanzrau.dv
registry: ghcr.io/ryanzrau/mono
letsencrypt_email: me@ryanzrau.dv

apps:
  ryanzrau:
    subdomain: ryanzrau
    enabled: true
    port: 80
