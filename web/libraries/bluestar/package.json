{
  "name": "bluestar",
  "version": "1.0.0",
  "description": "Lightweight React component library with goober styling and flexible theming",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs --out-dir dist",
    "prepare": "npm run build",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },
  "license": "ISC",
  "dependencies": {
    "goober": "^2.1.18"
  },
  "peerDependencies": {
    "react": "^18 || ^19"
  },
  "devDependencies": {
    "@storybook/addon-docs": "^10.0.7",
    "@storybook/react-vite": "^10.0.7",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "storybook": "^10.0.7",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.2"
  }
}
